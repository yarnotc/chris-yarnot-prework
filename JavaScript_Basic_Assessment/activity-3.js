let a_variable= prompt("input something to see if its longer than 4 characters");
if(a_variable.length > 4){
	alert('"'+a_variable + "\" is greater than 4 characters")
}else{
	alert('"'+a_variable+"\" is less than (or equal to) 4 characters")
}